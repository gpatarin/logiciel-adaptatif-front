import React from 'react';
import { idText } from 'typescript';

class clients {
    state = {
        name: idText,
        type: String,
        firsName: String,
        lastName: String,
        mail:String,
        createdAt: Date,
    }

    list = {
        
    }
}

const Client = () => {
    return (
        <div>
            
        </div>
    );
};

export default Client;