import React from 'react';
import { createSemanticDiagnosticsBuilderProgram, ObjectFlags } from 'typescript';

class produits {
    state = {
        id: String,
        name: String,
        stock: Int32Array,
        image: image,
        price: double
    }
}

const product = (props) => {
    return (
        <div>
            
        </div>
    );
};



export default product;